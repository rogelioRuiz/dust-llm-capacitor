{
  "name": "capacitor-llm-example",
  "version": "1.0.0",
  "private": true,
  "description": "E2E test app for capacitor-llm plugin",
  "scripts": {
    "sync": "cap sync",
    "android": "cap run android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/ios": "^8.1.0",
    "dust-llm-capacitor": "file:..",
    "dust-core-capacitor": "^0.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
